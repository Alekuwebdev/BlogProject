<%- include('../partials/head.ejs') %>
<body>
    <%- include('../partials/nav.ejs') %>

    <% if(blogs.length > 0) { %>
        <% blogs.forEach(blog => { %>
            <a href="/blogs/<%= blog._id %>">
                <h1><%= blog.title %></h1>
                <p><%= blog.snippet %></p>
            </a>
        <% }); %>
    <% } %>

    <%- include('../partials/footer.ejs') %>
</body>
</html>