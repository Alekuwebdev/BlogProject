<%- include('./partials/head.ejs') %>
<body>
    <%- include('./partials/nav.ejs') %>

    <% if(blogs.length > 0) { %>
        <% blogs.forEach(blog => { %>
            
            <h1><%= blog.title %></h1>
            <p><%= blog.snippet %></p>

        <% }); %>
    <% } %>

    <%- include('./partials/footer.ejs') %>
</body>
</html>